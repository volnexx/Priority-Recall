{
  "name": "term-interval-review",
  "version": "0.37.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "check": "npm run build && node --check main.js && npm test"
  }
}
